<%= form_for(@rhua) do |f| %>
  <% if @rhua.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rhua.errors.count, "error") %> prohibited this rhua from being saved:</h2>

      <ul>
      <% @rhua.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :hostname %><br />
    <%= f.text_field :hostname %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
